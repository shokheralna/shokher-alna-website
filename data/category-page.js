document.addEventListener("DOMContentLoaded",()=>{const category=document.body.dataset.category,products=window.SHOKHER_ALNA_PRODUCTS||[],grid=document.getElementById("categoryProductGrid"),empty=document.getElementById("categoryEmpty");if(!grid||!empty||!category)return;const visible=products.filter(p=>p.category===category&&(p.status==="active"||p.status==="out-of-stock"));const priceText=p=>p.price?`$${p.price}`:"Contact for price";function createCard(p){const out=p.status==="out-of-stock",imagePath=`../${p.folder}/main.jpg`,url=`product.html?id=${encodeURIComponent(p.id)}`,card=document.createElement("article");card.className="product-card";card.innerHTML=`<a href="${url}" style="color:inherit;text-decoration:none"><div class="product-image product-image-file"><span class="product-badge ${out?"product-badge-dark":""}">${out?"Out of Stock":"Available"}</span><img src="${imagePath}" alt="${p.name}" loading="lazy"><div class="image-fallback" hidden><span>Upload main.jpg</span><small>${p.name}</small></div></div><div class="product-card-body"><div class="product-card-topline"><span class="product-category">${p.category.replaceAll("-"," ")}</span><span class="product-price">${priceText(p)}</span></div><h3>${p.name}</h3><p>${p.description}</p><span class="product-order-link">View photos →</span></div></a>${!out&&p.orderEnabled?`<div class="product-card-body" style="padding-top:0"><a class="product-order-link" href="${window.buildWhatsAppOrderUrl(p)}" target="_blank" rel="noopener">Order via WhatsApp →</a></div>`:""}`;const img=card.querySelector("img"),fallback=card.querySelector(".image-fallback");img.addEventListener("error",()=>{img.hidden=true;fallback.hidden=false});return card}if(!visible.length){empty.hidden=false;return}visible.forEach(p=>grid.appendChild(createCard(p)))});
